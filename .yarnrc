# Yarn configuration file
# This file configures Yarn behavior for this workspace

# Set the default registry (optional, defaults to npm registry)
registry "https://registry.yarnpkg.com"

# Disable the emoji in output for cleaner logs
--emoji true

# Save exact versions instead of ranges
--exact true
